# testprog.server

Server-side runtime for hosting test suites and evaluating student implementations.

## What it provides

- Configuration loader for server/test-suite setup
- Server host lifecycle and runtime events
- Execution pipeline for validation and result reporting

## Install

```bash
dotnet add package testprog.server
```

