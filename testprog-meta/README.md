# testprog

Convenience meta-package that references the full `testprog` stack:

- `testprog.messenger`
- `testprog.client`
- `testprog.server`

Use this package when you want one package reference in your project file but still rely on the official split packages internally.

## Install

```bash
dotnet add package testprog
```
