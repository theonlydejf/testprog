# testprog.client

Student-facing API for connecting to a `testprog` server, executing tasks, and streaming results.

## What it provides

- `StudentConsoleTestRunner` for orchestrating runs
- `StudentClientOptions` for transport/discovery setup
- Integration with `testprog.messenger` protocol contracts

## Install

```bash
dotnet add package testprog.client
```
